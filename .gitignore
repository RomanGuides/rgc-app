# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
playwright-report
test-results
blob-report

# Variabili d'ambiente — contengono chiavi API, mai da versionare
.env
.env.*
!.env.example

# Altri formati comuni di credenziali/certificati — nessuno presente oggi,
# regola difensiva per quando il progetto crescerà
*.pem
*.key
*.p12
*.pfx
*.mobileprovision
credentials.json
service-account*.json

# Chiave di firma Android (upload key per Play App Signing, 2026-08-18) —
# CHANGELOG.md dichiarava queste due regole già presenti, ma non lo erano
# davvero: trovato e corretto prima di generare la chiave reale, non dopo.
*.jks
*.keystore

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Screenshot/marketing assets for store listings — working files, not app source
store-assets/
