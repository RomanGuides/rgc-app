# Changelog

All notable changes to this project are documented in this file, starting from the v1.0.0 baseline. Format loosely follows [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Added
- **Repeat-booking discount banner restored** (`ExperiencesScreen.tsx`, top of tab, below the masthead) — real copy/code (`ROME10`) from `docs/parked-content.md`, parked (not deleted) during the five-section Experiences restructure since it didn't fit that spec at the time. Same placement as originally shipped.
- **Discount popup on Home** (2026-08-17) — surfaces the same `ROME10` discount earlier, on the tab most people actually open first, instead of only inside Experiences. Shows once per install (same persisted-flag pattern as `WelcomeScreen`, new `hasSeenDiscountPopup` in `usePlacesStore.ts`), ~700ms after Home mounts so it doesn't feel like an instant ambush. Deliberately **different copy** from the Experiences banner: that banner's "Thank you for touring with Roman Guides" presumes a past booking, but the app has no accounts or booking records to actually check that — the popup uses generic wording ("Save 10% on your Roman Guides experience") so it makes sense for anyone, first-time visitor or not. "See Tours →" closes the popup and switches to the Experiences tab in-app (a deliberate choice over the existing banner's external link, so the discount hunt doesn't leave the app when the app already has the real tour list). The discount code itself (`ROME10`) is now a single shared constant (`src/config/promotions.ts`) referenced by both the popup and the Experiences banner, so a future code change can't update one and silently miss the other — the surrounding copy stays separate since it's genuinely different per placement, not duplicated text. **A real product trade-off, not free upside:** discovered while designing this that the app cannot distinguish first-time from repeat visitors at all (no accounts, bookings handled entirely by Bokun outside the app) — considered and rejected building an actual lead-capture flow (email/WhatsApp gated behind the code, submitted to a Google Form) for this same slot, given it would require a Privacy Policy update, explicit consent UI, and accepting a mechanism with no way to detect delivery failures (a bare `no-cors` POST to a Google Form endpoint); left as a documented option for later, not built.
- **Six new bookable tours added (2026-08-18), catalog grows from 7 to 13.** Sourced from the founder's own product Google Sheet (title, description, inclusions/exclusions, pricing, meeting points, cancellation policy) cross-checked against each tour's real Bokun `bookingUrl` (fetched and title-matched one by one, since Bokun's booking pages are JS-rendered and can't be scraped for details — same limitation the original 7 tours hit, resolved the same way: real figures confirmed against the source, not derived/estimated). Added: **Pasta and Tiramisu Cooking Class with Prosecco**, **Monti Neighborhood Food Tour**, **Rome Underground Tour: Catacombs, Crypts & Secrets**, **Pompeii Only Day Experience**, **Pompeii & Reggia di Caserta Guided Day Trip**, **Pompeii & Vesuvius Winery Day Trip**. A seventh sheet product ("Rome to Pompeii & Amalfi", the Amalfi-inclusive variant) has no Bokun link yet and was deliberately left out rather than shipped unbookable — added the moment the founder has it. Sheet product names carried an internal "Option 1"/"Option 2" labelling scheme not meant for customers; dropped per the founder's direction. `idRequired` and a couple of other optional fields were left unset rather than guessed where the sheet didn't state them, consistent with this project's no-invented-data rule for `TourDetailScreen`'s "Good to know" section.
  - **New `TourType` value: `'day-trip'`.** All three Pompeii-area tours were initially filed under `classic-tour` (matching the sheet's rough shape) but a 10-12 hour excursion outside Rome doesn't belong in the same bucket as a 1-hour Colosseum tour — the founder's own call once the mismatch was raised. `Pompeii & Vesuvius Winery` moved twice during this same conversation (food-tour, per its wine-tasting angle, then day-trip once the founder decided tour *structure* mattered more than its food component for grouping purposes) — both were explicit founder decisions, not assumptions.
  - **New cover-photo pipeline for tours without a CMS-hosted image yet.** Every existing tour's `imageUrl` points to a photo already uploaded to romanguides.com's media library; the 6 new tours' photos weren't uploaded there yet, so the founder asked to bundle them directly into the app instead of waiting on that step. New `getExperienceImageUrl()` helper (`experiencesService.ts`) checks the CMS `imageUrl` first, falling back to a new `EXPERIENCE_IMAGE_OVERRIDES` map (`src/data/experienceImageOverrides.ts`) of statically-imported local assets — same pattern as `PlaceScreen.tsx`'s category placeholder photos, so a missing file fails the build instead of silently 404ing. All three of `HomeScreen.tsx`'s carousel, `ExperiencesScreen.tsx`'s tour cards, and `TourDetailScreen.tsx`'s hero now read through this helper instead of `exp.imageUrl` directly. Source photos (from Bokun's own CDN, `imgcdn.bokun.tools`) were fetched at their native ~2600px width (250-750KB each) and resized/re-compressed to 1200px/quality 78 (58-157KB each, in line with the app's other bundled photos) using a one-off local `sharp` install (`npm install --no-save`, removed again after use — not a project dependency).
- **CI pipeline** (`.github/workflows/ci.yml`, 2026-08-18) — `npm run build` + the full Playwright e2e suite on every push/PR to `master`, closing the `ROADMAP.md` CI/CD gap. Requires a `VITE_ORS_API_KEY` repository secret (Vite bakes it in at build time) or the Directions e2e test fails — confirmed by watching the very first run fail on exactly that, then re-run green once the secret was set.
- **Play Store listing copy** (`docs/StoreListing.md`) — app name, short/long description, category, all written from what the app actually does today, plus a checklist of what's still missing before submission (fresh screenshots, feature graphic, a public-URL privacy policy page — the in-app Legal & About screen alone doesn't satisfy Play Console's Data Safety form).
- **Android upload keystore generated** (`android/keystore/roman-guides-upload.jks`, gitignored, not in this repo) — RSA 2048, 30-year validity, under the real BEN SRLS/Roman Guides identity. Play App Signing means this is an upload key, not the final app-signing key (Google holds that); losing it is recoverable via Google's reset process, not catastrophic, but it's still not backed up anywhere but the founder's own machine yet.
- **Brand evolution, Phase 2 (2026-08-17): design tokens, typography, and logo.** A full design-system audit (tokens, all shared components, every screen) preceded this — see the session record for the complete findings (duplicated "Fact"/badge/pill components across files, ~13 hardcoded hex colors drifted from their nearest token, three near-identical off-white literals with no shared token, etc.). This phase covers only the token layer and the logo; the per-screen literal→token sweep is future work (later phases), tracked but not yet scheduled.
  - **Real conflict found and resolved before implementing:** the project's own official brand kit (`BrandBoard.pdf`, made on LOGO.com) specifies red `#ff0033`, pure black, a `#006600` green accent (never used anywhere in the app), and Vollkorn Bold — which is exactly what `tokens.css` already implemented. The new branding-evolution mockup the founder shared specifies different values entirely (deeper red, warm neutrals instead of pure black/white, no green, Playfair Display instead of Vollkorn) — a real evolution beyond the original kit, not a refinement of it. Founder confirmed explicitly: adopt the new mockup's values, retire the green accent entirely.
  - `tokens.css`: `--red` `#ff0033→#e30613`, `--red-dk` `#cc0029→#b8050f` (computed ~20% darker, for hover), `--ink` `#2a1210→#1a1a1a` (Charcoal — coincidentally very close to `#1A1614`, the hex literal already used ad hoc in ~15 places across the app, found during the audit), `--stone` `#8a6f6c→#6b6866` (Warm Grey), `--bg-app` `#fbf1f0→#faf7f3` (Warm Ivory), `--surface-2` `#f5e4e2→#fde7e9` (Roman Blush — its existing role, soft secondary surfaces/selected states, was already what blush is for, so the existing token was evolved in place rather than adding a parallel one). `--display` swapped from `'Vollkorn'` to `'Playfair Display'`; `index.html`'s Google Fonts request updated to match (fonts are loaded from Google Fonts, not self-hosted, so this was a one-line change with no licensing gap). Every screen/component that already consumed these tokens re-skinned automatically with zero other file changes — confirmed on a real device.
  - New shared `BrandMark` component (`src/design-system/components/BrandMark.tsx`) — the real logo (`src/assets/brand/roman-guides-logo.png`, sourced from the founder's brand asset kit, color-on-transparent variant), not just text, at the top of Home/Experiences/Saved. Sized at 96px tall after two real-device rounds of "bigger" feedback (started at 36px). Deliberately **not** on Rome: it's a full-bleed map with no reserved header today, and adding one would shrink the map area — founder confirmed explicitly this tab stays untouched. Home's treatment is a further iteration on top of that: the logo sits directly on the hero photo (absolutely positioned, no background bar) rather than in the same boxed bar Experiences/Saved use — a specific founder request, tried and kept after a device check.
- **Privacy Policy/Terms of Service draft strengthened** (`src/config/legal.ts`) with the two pieces GDPR requires that the original draft was missing: a "Who we are" section naming the actual data controller (BEN SRLS, VAT/Tax ID IT14780641008, Via dei Santissimi Quattro 77, 00184 Roma, trading as Roman Guides — identity provided by the founder, 2026-08-17) and a "Your rights" section (access/correction/deletion/portability/objection, plus the right to complain to Italy's Garante per la Protezione dei Dati Personali). Terms of Service now names the same operator identity. Still explicitly a strengthened **draft**, not a legal review — `ROADMAP.md`'s existing item (lawyer review required before any App/Play Store submission) is unchanged by this.
- Capacitor native wrapper for iOS and Android (`ios/`, `android/`, `capacitor.config.ts`), enabling native App Store/Play Store builds of the existing web app. No app logic changed — geolocation still uses the standard browser API, not a native plugin (deferred to a future change). Location permission strings declared on both platforms (`NSLocationWhenInUseUsageDescription` on iOS, `ACCESS_COARSE_LOCATION`/`ACCESS_FINE_LOCATION` on Android).
- `npm run cap:sync` / `cap:ios` / `cap:android` scripts for the native build workflow.
- `.gitignore` hardened for native signing material: `*.mobileprovision` at the time. **Correction, 2026-08-18**: this line originally also claimed `*.jks`/`*.keystore` were covered — they weren't, a real gap only found while about to generate the actual Android upload keystore. Added both patterns then, verified with a real test file before trusting it.
- `@capacitor/dialog` dependency, used in place of `window.alert()` (see Fixed below).
- Redesign v1 — "Empty and Error States" addendum: search-no-match suggestion pills (`SearchScreen.tsx`, computed entirely from the bundle via Levenshtein name-matching, `src/utils/levenshtein.ts`), zero-category-results messaging with two distinct recovery actions (`RomeSheet.tsx`), a by-neighbourhood grouped view replacing "Nearest to you" when location is explicitly denied (new `'denied'` status in `useGeolocation.ts`, distinct from a timeout/unsupported `'fallback'`), an offline banner + permanent map background color so failed tiles never show a grey checkerboard (`useOnlineStatus.ts`, `OfflineBanner.tsx`), and a broken-image fallback on `PlaceScreen.tsx`'s header. `EmptyState.tsx` reworked to the addendum's visual rules (left-aligned, 40px outline icon, text actions instead of buttons).
- In-app Bokun booking widget (`BookingWidgetModal.tsx`): "Discover Experience" on the Experiences tab now opens Bokun's hosted checkout inside a full-screen in-app view instead of handing off to the system browser. Uses Bokun's own official embed mechanism (their `BokunWidgetsLoader.js` script, loaded once per app session, plus a `<div class="bokunWidget" data-src="...">` placeholder that their script detects and takes over) — no Capacitor plugin needed, since the whole app already runs inside one WebView. Bokun remains fully responsible for the booking and payment flow — no backend, no API keys, no PCI scope taken on for this. See `docs/BokunIntegration.md` for the full comparison against a native REST v1 checkout and why the embedded widget was chosen for now.
- `scripts/check-place-areas.mjs`, run at the start of every `npm run build`: fails with a clear message if any place's `area` is missing or outside the 27 hand-confirmed canonical neighbourhood names, so a future Google Sheet re-export can't silently wipe that manual work (see `docs/DataModel.md`). All 89 places' `area` values were normalized to this canonical list as part of this change (16 places corrected — the rest were already consistent from an earlier pass); no other field changed.
- Experiences tab restructured around five sections (masthead, the seven tours, Meet the Guides, What guests say, Our Story) — it now also carries the "who we are" content that used to live on the Saved tab (My Rome). Saved tab (`MyRomeScreen.tsx`) reduced to the shortlist only: title, count, and the saved-places list, now sorted by distance. Structure/layout only for the Experiences move — final copy (masthead phrase, guide bios, quotes) still pending; content that didn't fit either screen's new structure (discount banner, alternate night-angle tour copy, per-guide WhatsApp button, TripAdvisor review link, "videos coming soon" card) was moved to `docs/parked-content.md` rather than deleted.
- Category filter icons (`CATEGORY_ICONS` in `Icons.tsx`): six new outline icons (utensils, pizza, gelato, rooftop bar, cocktail bar, gallery) replace the emoji in `RomeSheet.tsx`'s filter popover — the last spot in the app still using emoji. Two design docs (`Icons Addendum`, `Handoff Diff`) explicitly say categories should keep their emoji and that no icon registry is wanted — confirmed with the team that guidance is superseded before building this. Not defined anywhere, so designed from scratch, matching the established 24×24/stroke-2/round-caps/no-fill convention.
- Category placeholder photos for `PlaceScreen.tsx`'s header: gastronomic categories (restaurant, pasta, pizza, gelato, rooftop_bar, cocktail_bar) fall back to a bundled photo when a place has no `imageUrl` of its own, tried after the place's real photo and before the flat `#F3EFEB` fallback. `gallery` (the one non-gastronomic category) keeps the flat fallback; a generic monument photo would be misleading. **Path correction, 2026-08-06:** this originally shipped as a runtime string path (`/assets/category/<category>.jpg`, implying `public/assets/category/`) since real photos didn't exist yet. Once real photos arrived, the content brief specified everything under `src/`, so this was rewritten to static Vite imports from `src/assets/category/<category>.jpg` instead — Vite guarantees the file exists at build time rather than 404ing silently at runtime. `public/assets/category/` was never correct and nothing was ever shipped there; the six files live at `src/assets/category/`, named exactly to the `PlaceCategory` keys (underscores, not hyphens: `rooftop_bar.jpg`, `cocktail_bar.jpg`).
- `@capacitor/app` dependency, used only so `BookingWidgetModal.tsx` can intercept the Android hardware back button (see Fixed below); no other use yet.
- `BookingWidgetModal.tsx` brought up to the full checkout-embed spec: a close (✕) control instead of a back chevron — closing a checkout is a dismissal, not a backward navigation step; a trust row under the tour name (padlock + hostname, 12px `#6E645F`) so the person about to enter a card has some visible signal the page is the real widget; a 3px indeterminate loading bar plus skeleton blocks on first load (never a blank white screen); and a dedicated error state — triggered by an explicit `onError` or a ~10s load timeout (iframes don't reliably fire `onError` for most real failures, so the timeout is the actual detection mechanism) — offering three actions: Try again, Open in browser, and a placeholder "Contact us" (`LINKS.SUPPORT_CONTACT_URL`, a `mailto:` placeholder pending the founder's real support address). Three actions on one screen is deliberately more than anywhere else in the app — a dead end here is a lost booking.
- **Verified limit, not an assumption:** reading a cross-origin iframe's current URL from the parent page is blocked outright by the browser's same-origin policy — a hard platform boundary, not a bug to fix. Since we supply the widget's `data-src` ourselves, the trust row's initial hostname is known synchronously and doesn't depend on this at all; the component also reads Bokun's own internal iframe's `src` attribute directly once it appears (an element in our own DOM, not a cross-origin document read) as a best-effort improvement if it navigates further. For the native "new window" relay case (see Fixed below), the URL likewise comes from our own code, not from peeking into an iframe. Only a navigation the widget makes entirely on its own, with neither of those signals available, falls back to a neutral "Secure payment page" rather than fabricating a hostname.
- **Legal & About screen** (`src/features/legal/LegalScreen.tsx`) — privacy policy, terms of service, app version, and contact, reachable from a plain text link in `RomeSheet`'s full-detent footer (next to Get Around/Emergency/Find Water Nearby), not a tab or a Settings screen — required before either app store listing can go live, since both require an in-app-reachable privacy policy and the app uses location. Copy lives in `src/config/legal.ts`, same pattern as `story.ts` (hand-written, not sourced from the Google Sheets content pipeline — legal text shouldn't be editable from a spreadsheet without review). **This copy is a draft reflecting actual app behavior (no accounts, on-device-only saves, OpenRouteService for directions, Bokun for booking/payment, no analytics/tracking), not a legally reviewed final text — tracked in `ROADMAP.md` as a publication blocker until reviewed.**
- Real support contact address confirmed by the founder: `LINKS.SUPPORT_CONTACT_URL` is now `mailto:info@romanguides.com`, replacing the `support@romanguides.com` placeholder used by `BookingWidgetModal`'s error state and now also `LegalScreen`'s contact line.
- **WelcomeScreen** (`src/features/welcome/WelcomeScreen.tsx`), shown once per install, before any tab. Previously the app requested location only on an explicit compass tap — no cold dialog at launch, but still a context-free ask the first time it happened, and on iOS the system dialog only shows once ever, so a reflexive "no" there is permanent. Full-screen hero photo (`appContent.json`'s existing "hero" entry), dark gradient over the bottom third for legibility even if the photo fails to load, "Rome, from people who live here." (reusing `OUR_STORY_MASTHEAD`), one line of body copy, a primary button ("Show me what's nearby") that triggers the location permission prompt either way and proceeds regardless of the answer, and a "Not now" text link that proceeds without asking at all. No carousel — deliberately one screen. Shown-once state lives in `usePlacesStore`'s existing persisted slice (`hasSeenWelcome`), not a separate localStorage key.
- `MapScreen.tsx` now checks `navigator.permissions.query({name:'geolocation'})` on mount and auto-requests location immediately if the permission is already decided (granted or denied — from Welcome, or a previous session), so "Nearest to you" (or the by-neighbourhood list, if denied) is populated right away instead of needing a compass tap first. Deliberately does **not** auto-request while permission is still `'prompt'` (undecided) — doing so would silently trigger the OS dialog after a "Not now", defeating the entire point of deferring it. Falls back to no auto-request if the Permissions API is unavailable, same as before this change.
- **`RomeSheet`: a resolved-but-distant location no longer shows "Nearest to you" with nonsensical distances.** If location access is granted but resolves far from Rome — at home before the trip, at the airport, anywhere more than 50km from the city center — the previous behavior sorted the 89 places by real distance anyway, surfacing entries like "9,000 km away". Reuses the same by-neighbourhood grouped list already built for the explicit-denial case (`RomeSheet.tsx`'s `areaGroups`), since the practical need is identical: real distance sorting isn't useful yet either way. 50km comfortably covers Rome's whole metro area (Fiumicino ~30km, Ciampino ~15km from center) without triggering for someone already in town.
- **Location "priming" outside first launch.** If the user tapped "Not now" on `WelcomeScreen` and later taps the compass (`LocateButton`) for the first time, the OS permission is still undecided (`'prompt'`) — previously that tap went straight to the system dialog with no context, and on iOS that dialog only shows once ever, so a reflexive "no" there is permanent. `MapScreen.tsx`'s `handleLocateMe` now checks `navigator.permissions.query` on tap: while still `'prompt'`, the first tap only shows a one-line explanation ("Turn on location to sort by how far away things are.") near the button instead of requesting anything; a second tap then actually asks. Once permission is decided (either way), behavior is unchanged — immediate request, same as before. Session-only, not persisted: a restart re-explains if still undecided, which is fine, since there's no dialog burned yet either way. **The hint renders below the button, not above as originally specified** — not enough vertical room above it for two lines of text without the hint itself overlapping and intercepting taps on the button (found via a failing Playwright test). Built and tested via Playwright without a physical device available overnight; confirmed working and readable on a real Android device the next day.
- **Gift cards, purchasable in-app.** A new "Give the gift of Rome" card on the Experiences tab, below the seven tours, opens the same in-app checkout used for tour bookings, pointed at Bokun's "Gift Card" widget product instead of an experience — same channel, same zero-backend/zero-PCI-scope mechanism, just a different Bokun product URL (`LINKS.GIFT_CARD_BOOKING_URL`). Required generalizing `BookingWidgetModal`'s props from a full `Experience` object to a minimal `BookableItem` (`{ id, name, bookingUrl }`, exported from the module) — those were the only three fields it ever used, and a gift card isn't really an "Experience" in the data-model sense. No behavior change for existing tour bookings. Verified end-to-end on a real Android device: the widget loads correctly for the gift-card product, same as it does for a tour.
- **Guide Detail screen** (`src/features/experiences/GuideDetailScreen.tsx`) — full bio, photo, and quote for a guide, reached by tapping their row in "Meet the Guides" (previously not tappable at all). The full bios existed nowhere in the app before this — the inline row only ever showed a one-line summary, now CSS-clamped to 2 lines (`-webkit-line-clamp`, text stays fully in the DOM for accessibility/tests) with the full text reserved for the detail screen. No contact/WhatsApp action on this screen — considered and explicitly declined for now, since it's a real response-time commitment, not just a content decision. `Guide.quote` is now optional (`src/data/types.ts`) — not every guide has a written pull-quote, and none should be invented.
- **Real guide roster, replacing three long-standing placeholder names.** `src/data/guides.json` had "Alex" and "Luca" since the Experiences restructure — placeholder names invented before real guide content existed. Their photo and quote fields, however, turned out to already be the *real* ones for Arjan and Giovanni (verified by comparing against the founder's actual bio text) — only the name/title/short-bio fields were wrong. Corrected to the real roster: **Eni** (the founder — "Endrin" is his legal name, "Eni" is how he's introduced to guests and the name used going forward), **Arjan**, **Giovanni** — now with their real, full bios instead of one-line placeholders — plus two guides missing from the app entirely: **Realda** and **Said**, added with real bios and no photo yet (renders as the existing initial-in-a-circle fallback, already supported, no code change needed).
- **Tour Detail screen** (`src/features/experiences/TourDetailScreen.tsx`) — tapping "Discover Experience" now opens a full tour detail (duration, price, ages, description, what's included/not included, meeting point, cancellation policy) before handing off to the Bokun checkout, instead of going straight to it. Long-deferred for lack of real structured data (only 3 of 7 tours had any description, none had duration/price/inclusions); unblocked once the founder confirmed real operational figures for all 7 tours directly on Bokun (2026-08-09/10). `Experience` (`src/data/types.ts`) gained `durationMinutes`, `price`/`priceNote`/`currency`, `ageRequirement`, `meetingPoint`, `description`, `inclusions`/`exclusions`, `cancellationPolicy` — all optional, a missing field collapses its row/section rather than showing an empty placeholder. "Check dates", not "Book now": the screen after this one is a calendar/cart, not an instant booking. First version used long, multi-paragraph descriptions pasted from marketing copy; shortened to 2-3 sentences per the founder's own on-device feedback ("too long, needs to be scannable"), and the redundant "Highlights" section (a duplicate of the description in list form) was dropped entirely rather than kept alongside a shorter description.
- **Rome tab UX audit (2026-08-16), 6 small isolated improvements** — a follow-up review that deliberately did NOT redesign the map/sheet/marker/PlaceScreen architecture, scoped to what the existing components and data already supported:
  - **Colosseum's real bookingUrl, exposed.** `Place.bookingUrl` existed in the data model for "the few places with a real Roman Guides tour linked," but exactly one place populates it (Colosseum, same Bokun product as the Colosseum Underground tour) and `PlaceScreen.tsx` never read it — that booking path was dead despite the data existing. "Book this tour" now shows (primary, reusing the same `BookingWidgetModal`/`BookableItem` as Experiences, no second implementation) whenever `bookingUrl` is set, with "Walk there" becoming the secondary/outline action in that case; every other place is unchanged.
  - **PlaceScreen enter/exit transition.** Previously an instant hard cut (no CSS transition at all) despite being documented as a "push" screen. Now slides in/out from the right (~200ms), matching a push/pop mental model rather than a modal fade. Exit needed a real fix, not just CSS: the store clears `selectedPlace` (from the back button, or from `startWalkingDirections.ts` when a route starts) before an exit animation could play, so `MapScreen.tsx` now keeps `PlaceScreen` mounted for the transition's duration via a local `placeScreenPlace`/`closing` state pair instead of unmounting instantly.
  - **Selected marker state.** Tapping a marker previously left no visual trace on the map — returning from `PlaceScreen`, there was no way to tell which pin you'd just viewed (relevant with several close together, e.g. the Fori area). The tapped marker now grows slightly with a soft red ring while its place screen is open (and through the closing transition, via the same `placeScreenPlace` state above). **Real bug found and fixed during this change:** the first version scaled the marker via `transform: scale(...)`, which collided with MapLibre's own `Marker._update()` (it overwrites `style.transform` on the same element for positioning on every render) — silently teleporting markers away from their real map position and breaking clicks, caught by a failing Playwright test, not by visual inspection. Fixed by animating `width`/`height` instead (safe: MapLibre never touches those), since the marker's percentage-based anchor offset recalculates automatically off the element's own size.
  - **"Tonight" now shows its own photo and links out.** `appContent.json`'s `tip_of_the_day` entry already had a real `imageUrl` and `ctaUrl` (an Instagram link) — `RomeSheet.tsx` only ever read `title`/`subtitle`, rendering it as plain, non-tappable text. Now shows a small thumbnail and wraps the block in the same external-link pattern already used for "Leave a review"/"Find Water Nearby".
  - **Filter button active-state indicator.** The category filter button looked identical whether every category was active or the user had filtered down to one — no visual cue of an active filter at all. Now shows a small red dot when `activeCategories` is a strict subset.
  - **Editorial block hierarchy.** "Local secret"/"Did you know"/"Nearby" (`PlaceScreen.tsx`'s `EditorialBlock`) shared identical typography with zero visual separation, blurring together into one wall of text when several were present on the same place. A subtle left border (not a full tinted box like "Insider tip") now gives each its own distinct callout.
  - Also replaced the last two raw emoji in the map's own chrome (🧭/📍 in `LocateButton.tsx` and the "me" marker) with the existing shared SVG icon set (`CompassIcon`/`LocationPinIcon` added to `Icons.tsx`, same 24×24/stroke-2 convention) — map markers for actual place categories are unchanged/still emoji, out of scope on purpose. **Second real bug found via a failing Playwright test:** `e2e/smoke.spec.ts`'s `placeMarkers()` helper excluded the "me" marker (no click handler) by checking its text for `📍` — replacing that emoji with an SVG (no text content) silently broke the exclusion, letting a test click land on the click-less "me" marker instead of a real place marker. Fixed by giving the "me" marker a stable `rgc-me-marker` class instead of relying on its rendered content.
- **New Home tab (4th tab, default on open)** — a scoped-down version of a moodboard the founder shared: reviewed and explicitly turned down as a full OTA-style storefront (notification bell, second search bar, fabricated tour names/categories), then rebuilt from the founder's actual ask — a "business card" first screen plus navigation shortcuts, built entirely from real, already-existing data:
  - Masthead reuses `appContent.json`'s `hero` entry (photo + "Meet your trusted Rome travel agency." + "Rated 5.0 by 10,000+ travelers") — previously shown exactly once ever, by `WelcomeScreen`, now visible every time the app opens.
  - "Top Experiences" — a horizontal carousel pushing every real tour (Best Sellers first), reusing `TourDetailScreen`/`BookingWidgetModal` — no second booking implementation. Scales automatically as the founder's incoming 6 new tours are added to `experiences.json`.
  - New `Experience.tourType` field (`'classic-tour' | 'experience' | 'food-tour' | 'cooking-class'`, see `docs/DataModel.md`) — a real product taxonomy, not the moodboard's invented categories, added because the founder is about to grow the catalog from 7 to 13 tours including its first food tours/cooking classes. The Experiences tab's tour list is now grouped by this field (empty categories, `food-tour`/`cooking-class` today, simply don't render a section); Home shows a pill row of whichever categories currently have tours, tapping one switches to Experiences.
  - Navigation shortcuts: Browse the Map (→ Rome), Gift Cards (→ opens the existing gift-card booking widget directly), Meet the Guides (→ Experiences, scrolled to the guides section specifically). Category pills (Classic Tours/Experiences/...) scroll to their own tourType section the same way — both go through one `onNavigateToSection(target: TourType | 'guides')` callback and a one-shot scroll-target state in `App.tsx`, separate from the generic tab-switch `onNavigate`.
  - "Why travel with us?" trust section and two review buttons (Google — same link now centralized in `LINKS.GOOGLE_REVIEW_URL`, previously inline only in Experiences — and TripAdvisor, `LINKS.TRIPADVISOR_REVIEW_URL`, confirmed by the founder).
  - **Three real bugs found across repeated on-device testing, none caught by Playwright until the tests were changed to actually check scroll position instead of just element presence:** (1) the scroll fired before `experiences`/`guides` (loaded asynchronously, empty on first render) had populated, landing on the still-empty, much-shorter layout — fixed by gating on `guides.length > 0`. (2) That alone still undershot on-device — `scrollIntoView`'s automatic nearest-scrollable-ancestor resolution turned out to be unreliable in this Android WebView against a nested `overflow:auto` container under a `position:fixed` root — replaced with directly measuring the target's offset and calling `scrollTo()` on the container ourselves. (3) The real culprit, found last: the effect called its `onScrollTargetHandled` callback *synchronously*, clearing `scrollTarget` in `App.tsx` immediately — which re-ran the same effect for its new value and fired that effect's own cleanup, **cancelling the animation frames before either had a chance to run**, so no scroll ever happened at all. Fixed by moving that callback to fire from inside the second animation frame, after the scroll had actually been kicked off. `e2e/smoke.spec.ts` gained a dedicated test asserting the target section's Y position lands near the top of the viewport after the tap — the only way to actually prove a scroll happened, since `toBeVisible()` can't distinguish "scrolled there" from "renders somewhere on this tall page" and had passed throughout all three bugs.
- **Ratings surfaced on the map and search.** `Place.rating`/`ratingCount` have been in the data model and populated for 86 of 89 places since the original Data Model, but nothing in the UI ever showed them. `PlaceScreen.tsx` now shows a small ★ rating + review count under the place name (collapses cleanly for the 3 places with no rating on file, same pattern as every other optional field on this screen); `SearchScreen.tsx`'s result rows show the same rating before the category/area/distance line. Scoped deliberately narrow after a broader "Rome tab redesign" moodboard was reviewed and explicitly turned down for everything else it proposed (always-visible category pills, colored marker icons, live open/closed status, price tiers, swipe-up in-place cards) — those either reverse decisions already made deliberately in this redesign, need data/APIs the app doesn't have, or lack a concrete problem driving them. This one piece was real, existing, unused data.
- **Tour Detail redesign, icon-driven style** (`TourDetailScreen.tsx`, `ExperiencesScreen.tsx`'s `TourCard`), replacing the plain-text v1 above after the founder preferred a richer visual reference shown on a real device: an icon-led facts row (duration/price), a "Who is it for?" callout box for the age requirement, a 2-column checkmark grid for "What's included" (replacing a plain bullet list), and a new "Good to know" section — wheelchair accessibility, ID/passport requirement, and health advisories (pregnancy, heart conditions, back problems), sourced per-tour from the founder-confirmed Bokun restriction data, not invented. New icons added to the shared icon set (`Icons.tsx`): `ClockIcon`, `TagIcon`, `CalendarIcon` (now also on the "Check dates" button), `CheckCircleIcon`, `WheelchairIcon`, `IdCardIcon`, `AlertCircleIcon`, `StarIcon` — same 24×24/stroke-2/round-caps/no-fill convention as the rest of the design system. `Experience` gained `wheelchairAccessible`, `idRequired`, `healthAdvisories`, and `bestSeller` (a manual founder pick, not derived from any real sales data the app has). Prices now read "From €X" rather than a bare figure. A "Best Seller" badge (Colosseum Underground, Colosseum Arena, Drunken History Rome) appears both on the tour list card and its detail screen. `formatDuration()` extracted to `src/utils/formatDuration.ts`, shared between the list card and the detail screen instead of being duplicated.
- **Brand evolution, Phase 2.5: real logo on every non-map tab.** Founder asked for the real logo image (not text) at the top of Home/Experiences/Saved after Phase 2's token/font work. New shared `BrandMark` component (`src/design-system/components/BrandMark.tsx`), the real logo asset (`src/assets/brand/roman-guides-logo.png`, sourced from the founder's brand asset kit, color-on-transparent variant). Sized at 96px tall after two rounds of real-device "bigger" feedback (started at 36px). Deliberately **not** on Rome: full-bleed map with no reserved header, and adding one would shrink the map area — founder confirmed this tab stays untouched. Home's treatment is a further iteration: the logo sits directly on the hero photo (absolutely positioned, no background bar) rather than in the same boxed bar Experiences/Saved use — a specific founder request, tried and kept after a device check.
- **Brand evolution, Phase 3: shared-component consolidation.** Three real duplication bugs found and fixed while wiring the new design tokens through the rest of the shared component layer:
  - `Button.tsx`'s unused `'gold'` variant removed entirely (confirmed zero call sites via grep) — narrowed the `variant` prop to `'primary' | 'ghost'`.
  - `BestSellerBadge` existed as three separate hand-rolled copies (`ExperiencesScreen.tsx`'s list card, `TourDetailScreen.tsx`, and a version drifted enough to hardcode `#1A1614` instead of the `--ink` token) instead of one shared component. Consolidated into a single exported `BestSellerBadge` in `Badge.tsx`; both screens now import it instead of re-implementing it.
  - `TabBar.tsx` had a second, locally-duplicated icon set (`ICON_PROPS` + 4 local icon functions) instead of using the shared `Icons.tsx` — the exact "second icon family" the design system already says not to have. Consolidated onto `Icons.tsx`, adding `HomeIcon`/`TicketIcon` there (`LocationPinIcon`/`HeartIcon` already existed and were reused as-is — `HeartIcon`'s path was byte-identical to the old local `MyRomeIcon`). `BrushRing` (the hand-drawn brush-circle around the active tab icon) left untouched — it already matches the real logo's brush motif.
  - **Real bug found during the `gold` removal**: Phase 2's red retoken (`#ff0033` → `#e30613`) had left two stale hardcoded literals of the *old* red behind — `Button.tsx`'s ghost-hover background and `Badge.tsx`'s `red` variant background/border, both still `rgba(255,0,51,...)` instead of referencing the token. Fixed both to the new red's decimal RGB (`rgba(227,6,19,...)`).
  - Five confirmed-dead files deleted (zero references anywhere in `src/`, verified via grep before removal): `SocialIcons.tsx`, `Chip.tsx`, `EmailCaptureBanner.tsx`, `BottomSheet.tsx`, and `src/features/myrome/MyRomePanel.tsx` (its only consumer). `npx tsc --noEmit` clean after deletion.
  - Deliberately deferred, flagged in code comments rather than fixed here: `Badge.tsx`'s unused `neutral` variant still uses `--surface-2`, which Phase 2 evolved from beige to pink (Roman Blush) — visually wrong for a "neutral" badge, but has zero live call sites today, so left as a Phase 5 token decision rather than improvised mid-refactor.
- **Brand evolution, Phase 4: Home composition polish.** Per the original Phase 1 plan, Phase 4 was scoped as "apply the visual direction on top of the already-approved Home architecture" — composition, not a rebuild (Home was already fully tokenized from Phase 2 onward). Three small `HomeScreen.tsx` fixes:
  - Removed a redundant "ROMAN GUIDES" text eyebrow that sat directly under the 96px logo added in Phase 2.5 — the logo itself already spells out the brand name, so the label doubled up on the same information right above the headline.
  - Fixed a stale hardcoded hero-overlay scrim color (`rgba(16,12,10,...)`, a pre-rebrand charcoal literal never updated when Phase 2 introduced `--ink` `#1a1a1a`) to its real decimal RGB (`rgba(26,26,26,...)`).
  - "Why travel with us?" card given a `var(--surface-2)` (Roman Blush) background instead of the default `var(--surface)` white, so it reads as a distinct warm editorial callout instead of blending into the page — the first deliberate use of the blush surface as a visual accent rather than just a hover/selected state.
- **Brand evolution, Phase 5: full per-screen hardcoded-literal → token sweep.** Per the original plan, this covered the 11 remaining screens/components not yet touched by Phases 2–4: `PlaceScreen.tsx`, `TourDetailScreen.tsx`, `RomeSheet.tsx`, `SearchScreen.tsx`, `SavedPlacesList.tsx`, `GuideDetailScreen.tsx`, `BookingWidgetModal.tsx`, `WelcomeScreen.tsx`, `LegalScreen.tsx`, `LocateButton.tsx`, and `MapView.tsx` (colors only, per the brief's explicit "don't redesign the map marker system" constraint). Five parallel audits (one per file group) inventoried every hardcoded color/font/spacing literal before any edit, so the sweep is evidence-based rather than a guess. Highlights:
  - **Old pre-rebrand color literals replaced app-wide**: `#FF0033`/`#ff0033` and `#CC0029`/`rgba(204,0,41,...)` (the pre-Phase-2 reds) → `var(--red)`; `#1A1614`, `#443A33`, `#8C7F79`, `#6E645F` (four different pre-rebrand "muted label"/"body text" hex values, all doing the same job) → `var(--ink)`/`var(--stone)`. Confirmed the correct mapping by checking `Button.tsx`'s own convention first (base fill = `--red`, hover = `--red-dk`) rather than guessing.
  - **`LegalScreen.tsx`'s 5 hardcoded `'Vollkorn, serif'` literals fixed** to `var(--display)` — a real, previously-undiscovered bug: this text (Privacy Policy/Terms of Service headings) has rendered in neither the old nor the new brand font since Phase 2 swapped the token to Playfair Display, because the hardcoded string bypassed the token entirely.
  - **Hairline/shadow alpha unified**: several files used a neutral `rgba(26,22,20, .07–.16)` for borders/shadows at inconsistent alphas instead of the design system's own `var(--line)`/`var(--shadow-color)` tokens (already used by `Card.tsx` and others) — consolidated to the existing tokens rather than left drifting.
  - **`#F3EFEB`** (a pre-rebrand neutral chip/callout background used inconsistently across `RomeSheet`, `SearchScreen`, `PlaceScreen`'s photo fallback, and `BookingWidgetModal`'s skeleton) consolidated to `var(--bg-app)` — numerically within a few RGB units of the same warm-ivory value, same role.
  - **`Badge.tsx`'s `neutral` variant removed** — zero call sites (confirmed via grep, same check repeated from Phase 3), left ambiguous rather than fixed back then since its `--surface-2` background became semantically wrong (pink, not neutral) once Phase 2 repurposed that token. Removed outright instead of guessing a color nobody uses, same precedent as `Button`'s `gold` variant removal.
  - **Same-file duplicate style objects extracted into local helpers** (same-file scope only, per the phase's own boundary — no new cross-file shared components): `PlaceScreen.tsx`'s two circular icon buttons and three CTA button variants, `TourDetailScreen.tsx`'s four repeated section wrappers and three repeated body-text blocks, `RomeSheet.tsx`'s repeated eyebrow-label/text-button/info-block patterns, `BookingWidgetModal.tsx`'s three repeated full-screen panel bases, `LegalScreen.tsx`'s repeated section-heading/paragraph styles.
  - **`SearchScreen.tsx`'s suggestion-pill `borderRadius: 99` fixed to `var(--radius-pill)`** (999px) — a likely typo (off by a factor of 10) that happened to look correct only because the pill is short enough to round fully either way.
  - **`MapView.tsx`, colors only**: confirmed by reading the file that the cluster/route-line colors are MapLibre GL *paint* properties, not DOM styles, so they cannot reference `var()` — they stay literal hex, recolored to the new red family (`#e30613`/`#b8050f`, plus a computed light tint `#ef6f76` preserving the old ramp's lightening ratio). The selected-marker glow (a real DOM style) follows `Button.tsx`'s existing pattern of hardcoding a token's decimal RGB with an explanatory comment. Zero structural/behavioral changes, per the map's standing "no redesign" constraint.
  - **Explicitly left untouched** (real, deliberate values — not drift): custom control heights (44/48/54px), the hero-photo scrim overlay (`rgba(16,12,10,...)`, unrelated to brand color), spacing values that don't land on the 4px scale (6/10/14/18/22/26/28px etc. — changing them would be a real, if small, layout shift, out of scope for a token-only sweep), skeleton-block mockup dimensions, and icon-specific tints with no token equivalent.
- **Brand evolution, Phase 6: final visual QA sweep.** Walked every screen touched by Phases 2–5 (Welcome, Home, Rome tab/RomeSheet/filter popover, Search, Experiences, Tour Detail, Guide Detail, Saved) in a real browser against the actual running app, screenshot by screen, rather than reviewing code alone.
  - **Real, previously-undiscovered bug found and fixed**: `src/index.css`, imported at the app's entry point (`main.tsx`), turned out to be untouched leftover boilerplate from the original `create-vite` scaffold — an entirely unrelated placeholder theme (purple accent color, `.counter`/`#social`/code-block styles, none ever used anywhere in this app, confirmed via grep) that nobody removed when the real Roman Guides Companion design system (`tokens.css`) was built. Its `#root` selector set `text-align: center` on the whole app root, silently centering all text app-wide since day one — invisible almost everywhere because most components fill their full container width or set their own alignment, but it broke `<ul>`/`<li>` bullet lists outright (the bullet marker and its text visually separated, found on `TourDetailScreen`'s "Not included" list) and subtly mis-aligned page headers, descriptions, and info blocks that were coded assuming ordinary left-aligned text. Deleted `src/index.css` and its import — zero other rules in the file were referenced anywhere in `src/` (no `h1`/`h2`/`.counter`/`#social` usage). Build's CSS output dropped from 72.76 kB to 70.98 kB, confirming the removed rules were genuinely dead weight, not load-bearing.
  - No other visual regressions found across the swept screens — Phases 2–5's token/color changes, extracted style helpers, and the Vollkorn font fix all render correctly.
  - `npx tsc --noEmit`, `npm run build`, and the full Playwright suite (27/27, including the dedicated Legal & About open/close test) all re-verified clean after the fix; confirmed on a real Android device.

### Fixed
- **Map screen, two real-device layout overlaps.** (1) MapLibre's `NavigationControl` (the zoom +/- buttons, top-right of the map) is anchored by the library at `top: 0` with no notion of the notch/status bar, so it rendered underneath the clock/battery icons — every other floating element above the map (`LocateButton`, `OfflineBanner`) already accounts for `env(safe-area-inset-top)`, this was the one left out. Fixed with a global CSS rule on `.maplibregl-ctrl-top-right` (`tokens.css`). (2) `RomeSheet`'s header (search bar + category filter button) sits at the sheet's top edge, which rises into the same top-right corner as `LocateButton` (🧭/📍) once the sheet reaches its `full` detent (90% of the screen) — `LocateButton`'s z-index is deliberately higher than the sheet's (needed for the filter popover, see below), so the filter button visually disappeared underneath the compass instead of the other way round. Fixed by fading `LocateButton` out (opacity + `pointer-events: none`) whenever the sheet reaches `full`, the same way Google Maps-style sheets hide floating controls once they dominate the screen — `RomeSheet` now reports its detent back to `MapScreen` via a new `onDetentChange` callback prop.
- **Bokun's own "Go to cart" button did nothing on tap**, reached right after choosing a tour's date/time — confirmed on a real device, not caught by any test or emulator. Real root cause, found through a live on-device debugging session with Chrome DevTools remote-inspecting the physical phone (`chrome://inspect`): the original implementation rendered Bokun's widget as a plain `<iframe src={bookingUrl}>`, but that's not Bokun's actual embed mechanism — their official embed (visible in the Bokun admin's widget-embed-code generator) is a `<script src=".../BokunWidgetsLoader.js?bookingChannelUUID=...">` plus a `<div class="bokunWidget" data-src="...">` placeholder that their own script detects (via a `MutationObserver`) and takes over, creating its own internal iframe and handling cart/checkout itself. A bare iframe never gave their code anything to hook into for that step: on-device testing proved the tap landed on exactly the right button (`document.elementFromPoint` at its true coordinates, ruling out an invisible overlay) and that even a direct `.click()` via the JS console produced zero effect — no DOM change, no network request, no console error, no `postMessage` — same as the real tap, ruling out a WebView touch/click-translation issue. Two earlier hypotheses (Capacitor's default `onCreateWindow` silently dropping `window.open()`; `shouldOverrideUrlLoading` treating nested-iframe navigation as an external Intent) were tried first and diagnostically disproven via native logging before the real cause was found — both fixes (`RomanGuidesWebChromeClient`, `RomanGuidesWebViewClient`) are kept anyway as general hardening for a real, separate risk: a payment popup (3-D Secure/PayPal/Apple Pay) opening from inside Bokun's own widget iframe. Fixed by switching `BookingWidgetModal.tsx` to Bokun's real embed mechanism — verified end-to-end on the same physical device: "Go to cart" now opens the real cart, and the flow proceeds through participant/contact details to the card-entry step. iOS has the same `onCreateWindow`-equivalent gap (`WKWebView`'s `WKUIDelegate.createWebViewWith`) for the payment-popup case only — not written in this pass, since this project has had no Mac/Xcode available to build or verify it (see `ROADMAP.md`).
- `PlaceScreen.tsx`'s header fell back to `meta.color` (the category's map-marker color) when a place had no image, or a broken one. For `restaurant`/`pasta` that color is `#ff0033` — close enough to brand red that a full-screen header in it reads as an error state. Changed the fallback to `#F3EFEB` (or a category placeholder photo first, see Added) — `meta.color` is no longer used for this at all.
- **Android hardware back button closed the whole app instead of the open screen** (2026-08-18, found on a real device). Root cause: this app has no routing library and never pushes browser history entries (Architettura v2, sezione 6), so Capacitor's default back-button behavior — fall back to `window.history.back()`, else exit — almost always exits, since there's usually nothing in history to go back to. Only `BookingWidgetModal` intercepted it before this fix; every other full-screen overlay (Tour Detail, Place, Guide Detail, Search, Legal & About, the Home discount popup) fell straight through to that default. New `useAndroidBackHandler` hook (`src/hooks/useAndroidBackHandler.ts`) fixes this properly rather than patching each screen with its own listener: a shared stack that each overlay pushes onto while mounted and pops on unmount, with a single global `App.addListener('backButton', ...)` in `App.tsx` that closes only the *topmost* one. A naive "every screen listens for itself" fix would have re-broken on the app's existing stacked cases (Tour Detail → its own Booking widget, Place → its own Booking widget): a single back press would have fired both listeners at once, closing two screens instead of one. Below the overlay stack, two more tiers: pressing back on a non-Home tab with nothing open now returns to Home instead of exiting; only pressing back again within 2 seconds while already on Home with nothing open actually exits the app (`App.exitApp()`) — matching standard Android app conventions, not exiting on a single stray press.
- **"Check dates"/"Book this tour" buttons had a large, growing gap of empty space below them** (2026-08-18, reported after the tap-anywhere-on-card fix below made people notice it more). Real root cause: `TourDetailScreen.tsx` and `PlaceScreen.tsx` both live inside the container `App.tsx` already clips to *above* the tab bar (`overflow: hidden`) — the tab bar itself, not these screens, is what borders the OS navigation bar and already carries its own `env(safe-area-inset-bottom)` padding. Both screens' bottom action bar padding added that same safe-area inset a second time on top of its own base padding, double-counting it and leaving a gap sized to whatever the device's real inset happened to be (larger on this test device than expected). Same bug, lower-visibility instances (end-of-scroll padding, not a fixed button bar) found and fixed the same way in `LegalScreen.tsx` and `GuideDetailScreen.tsx`. **Deliberately left unchanged**: `DirectionsBar.tsx` and `RomeSheet.tsx` use a similar-looking pattern, but both are floating/sheet elements where some bottom clearance may be an intentional part of how they "float" above the tab bar, not obviously the same bug — flagged, not touched, pending confirmation.
- **Tapping an Experiences tour card required hitting the "Discover Experience" link specifically** — the rest of the card (photo, title, price) did nothing. `HomeScreen.tsx`'s Top Experiences carousel already made its whole card tappable; `ExperiencesScreen.tsx`'s `TourCard` didn't. Added `onClick` to the whole `Card` (only when a real `bookingUrl` exists — a tour without one still only opens the external link via its own button, unchanged) so the whole card responds now, matching Home's existing behavior.
- `RomeSheet.tsx`'s category filter popover rendered inside the sheet's own `overflow: hidden` box, which also establishes its own stacking context at a lower z-index than `LocateButton`'s. Its local `zIndex: 6` could never out-rank `LocateButton` (z-index comparisons don't cross stacking-context boundaries) and got clipped/covered by it whenever the sheet was tall enough for the two to overlap. Rewritten to render through a `createPortal` into `document.body`, positioned from the filter button's real `getBoundingClientRect()` (flips to open upward if there isn't enough room below), with a full-screen transparent backdrop that closes it on tap.
- `BookingWidgetModal.tsx` had no Android hardware-back handling, so pressing it would fall through to Capacitor's default behavior — navigating the embedded iframe's own history (unexpected, and could silently step backward through a checkout flow) rather than closing our modal. `@capacitor/app`'s `backButton` listener, registered only while the modal is mounted, now closes it directly; no effect on iOS/web, where that event never fires.
- Found only by testing a real installed Android build (not caught by Playwright or browser testing): the bottom tab bar and the Map screen's top black bar had no safe-area padding, so on a device with a 3-button navigation bar and a standard status bar, both were rendered underneath system UI — the tab bar became untappable, and the top bar sat behind the clock/signal icons. Fixed with `env(safe-area-inset-bottom)` / `env(safe-area-inset-top)` padding in `src/App.tsx` and `src/features/map/MapScreen.tsx`.
- Also found only on a real device: `startWalkingDirections.ts` used `window.alert()` for its two user-facing error messages (no location set, route request failed). In Capacitor's native Android WebView this caused the app to freeze, requiring a force-close — a known incompatibility between raw browser dialogs and native WebViews. Replaced with `@capacitor/dialog`'s `Dialog.alert()`, which has an identical web fallback (no change in browser/Playwright behavior) but uses a proper native dialog on Android/iOS.
- The route-line effect in `MapView.tsx` depended on the whole `activeRoute` object, which gets a new reference on every location update while a route is active (`updateRouteProgress`) — this re-ran `map.fitBounds()` on every GPS tick, which was harmless indoors but saturated the render thread outdoors with frequent real GPS updates, causing an ANR requiring a force-close. Narrowed the dependency to `activeRoute?.destinationId`.
- `useGeolocation.ts`'s continuous `watchPosition` (used during an active route) briefly went through `enableHighAccuracy: false` as an attempted mitigation for the ANR above, on the theory that our own high-accuracy request was contributing to device-wide CPU/GPS contention. On a real device this produced a ~200m position error (WiFi/cell-based positioning instead of real GPS) — enough to break arrival detection (25m threshold). Reverted to `enableHighAccuracy: true`: the ANR's actual CPU breakdown showed other background apps and `system_server` as the dominant load, not this app's own accuracy setting, so the precision cost wasn't justified by the marginal ANR benefit.
- Starting a walking route froze the app immediately on a real device (confirmed via `adb logcat`: a brand-new GPS watch registered with the OS every ~15-20ms, non-stop). Root cause was the same class of bug as the `MapView.tsx` fix above, in a different effect: `MapScreen.tsx`'s tracking effect (`startWatching`/`stopWatching`) depended on the whole `activeRoute` object, which gets a new reference on every position update — each new reference re-ran the effect's cleanup and body, registering a fresh `watchPosition` in a tight loop, made worse by the `enableHighAccuracy: true` revert above producing far more frequent real position updates than before. Narrowed the dependency to `activeRoute?.destinationId`, same fix as `MapView.tsx`.

### Removed
- `src/features/home/` (`HomeScreen.tsx`, `homeConfig.ts`) — the pre-redesign Home tab, unrouted since the 5→3 tab cut (`App.tsx` only ever mounted `map`/`experiences`/`myrome`) but never deleted. Confirmed zero references anywhere else in the codebase before removal; bundle output (`dist/assets/index-*.js` size) was unchanged, confirming it was never actually reachable from the shipped app.

## [1.0.0] — 2026-08-01

### Added
- First Playwright end-to-end test suite for this app: Home, Map (basemap + markers), Explore, My Rome, marker popup, Directions (full flow: locate → select place → get directions → route drawn → stop route), and a layout-regression test for the email capture form's "Last name" field.
- Optional HTTPS-enabled local preview (`HTTPS=true npm run preview -- --host`), via `@vitejs/plugin-basic-ssl`, needed to test real GPS geolocation from a phone on the local network (mobile browsers require a secure context for the Geolocation API).
- `.gitignore` entries for Playwright artifacts (`playwright-report`, `test-results`, `blob-report`) and for common credential/certificate file patterns.

### Fixed
- Map: a fallback timer could call `map.addSource()` before the style finished loading, throwing "Style is not done loading." This silently broke both marker clustering and the Walking Directions route line, since both are set up in the same function. Fixed by checking `map.isStyleLoaded()` before proceeding, deferring to the real `load` event otherwise.
- Email capture form: the "Last name" input was clipped on narrow phone screens, a standard flexbox `min-width: auto` issue on two `flex: 1` inputs in a row. Fixed with explicit `minWidth: 0`.
- Geolocation: the one-shot `requestLocation()` call used a 6-second timeout, too short for a real first GPS/network fix on a physical device (especially indoors) — it reliably timed out and silently fell back to the hardcoded Trevi Fountain reference point. Raised to 15 seconds; confirmed fixed on a real phone.

### Removed
- Dead code, verified unreferenced anywhere in the codebase before removal: `src/services/collectionsService.ts`, `src/data/collections.json`, `src/utils/directions.ts`, `src/assets/` (unused template assets: `hero.png`, `react.svg`, `vite.svg`), `public/icons.svg`.
- Unused `catch` binding in `scripts/check-bundle-size.mjs`.

### Documentation
- Added `RELEASE_NOTES.md`, `docs/` (Architecture, Data Model, Map, Routing, Google Sheets, Deployment), `CONTRIBUTING.md`, and `ROADMAP.md`.
