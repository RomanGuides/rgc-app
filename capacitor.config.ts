import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.romanguides.app',
  appName: 'Roman Guides Companion',
  webDir: 'dist',
};

export default config;
